CREATE SCHEMA FRIDGEZONE;
CREATE TABLE item(
ID INT PRIMARY KEY AUTO_INCREMENT,
 NAME VARCHAR(255),
  QUANTITY INT NOT NULL,
  create_by VARCHAR(25),
   create_date TIMESTAMP default CURRENT_TIMESTAMP
   );